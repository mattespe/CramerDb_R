% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crud.R
\name{upsert}
\alias{upsert}
\title{Upsert records via API (PATCH if id exists; POST otherwise)}
\usage{
upsert(
  url,
  data,
  headers = list(),
  id_col = "id",
  style = c("auto", "plain", "feature"),
  chunk_size = 200L,
  base_url = "https://cramerdb.com/api/",
  dry_run = FALSE,
  verbose = getOption("cramerdb_verbose", FALSE),
  timeout = 60L,
  max_tries = 3L,
  on_missing = c("create", "error"),
  on_error = c("stop", "continue")
)
}
\arguments{
\item{url}{Endpoint - full URL or relative path (e.g. \code{"seine/event/"}).}

\item{data}{\code{data.frame} or \code{sf} object.}

\item{headers}{Named list of additional HTTP headers.}

\item{id_col}{Name of the primary key column (default \code{"id"}).}

\item{style}{\code{"auto"}, \code{"plain"}, or \code{"feature"} (GeoJSON). \code{"auto"} picks
\code{"feature"} for \code{sf} objects, \code{"plain"} otherwise.}

\item{chunk_size}{Controls progress message grouping (each row is still one request).}

\item{base_url}{Character. Base API URL.}

\item{dry_run}{Logical. Preview what would be sent without sending.}

\item{verbose}{Logical. Print per-chunk progress. Defaults to
\code{getOption("cramerdb_verbose", FALSE)}.}

\item{timeout}{Integer. Request timeout in seconds. Default 60.}

\item{max_tries}{Integer. Maximum retry attempts for transient errors (429, 503). Default 3.}

\item{on_missing}{\code{"create"} (default) or \code{"error"}; what to do when an id
returns 404.}

\item{on_error}{\code{"stop"} (default) or \code{"continue"}; whether a row the server
rejects aborts the batch or is skipped and reported in a warning.}
}
\value{
Invisibly returns \code{TRUE}, or \code{FALSE} when \code{on_error = "continue"}
and some rows failed.
}
\description{
A record whose id is not found is created instead. The API answers "no
such record" and "that record is not yours to see" with the same 404
status. A record you lack permission on therefore looks identical to one
that does not exist, and creating it duplicates the original instead of
updating it. Rows that hit this case are reported in a warning. Set
\code{on_missing = "error"} to stop on them instead.
}
